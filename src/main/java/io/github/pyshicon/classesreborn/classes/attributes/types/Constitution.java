package io.github.pyshicon.classesreborn.classes.attributes.types;

import io.github.pyshicon.classesreborn.classes.attributes.Attribute;

public class Constitution extends Attribute {

    protected Constitution(int point) {
        setName("Constitution");
        setPoint(point);
    }
}
